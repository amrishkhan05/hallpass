# Changelog

## 0.1.0

- Initial local-first policy engine, instruction discovery, Git diff normalization, deterministic detectors, approvals, conflicts, persona rendering, agent hook adapters, CLI, CI, and dual-registry release workflow.
