// .npmrc for Trusted Publishing
// Use the NPM_TOKEN secret from GitHub Actions
// This file ensures npm uses the token when publishing to npmjs.org
// and enables provenance for reproducible builds

//registry.npmjs.org/:_authToken=${NPM_TOKEN}
provenance=true
